const { connect, connection } = require('mongoose');

//connect to mongoDB with mongoose
//TODO insert route into connect method to make server run
mongoose.connect('');

module.exports = connection;
